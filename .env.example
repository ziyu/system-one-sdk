# This file is never loaded by the SDK itself. Pass configuration explicitly.
# TypeSafe direct / native-compatible System One API:
SYSTEM_ONE_API_KEY=
# Optional: override only for a proxy or a compatible service.
# SYSTEM_ONE_BASE_URL=https://api.typesafe.ai/v1
# Optional: a provider's exact model ID, or a pinned model version.
# SYSTEM_ONE_MODEL=jev-latest
